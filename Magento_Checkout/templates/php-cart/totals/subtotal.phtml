<template x-if="segment.code === 'subtotal'">
    <div :class="wrapperClass">
        <span x-text="segment.title"></span>
        <span x-text="hyva.formatPrice(segment.value)" class="justify-self-end"></span>
    </div>
</template>
